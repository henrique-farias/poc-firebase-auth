import * as React from 'react'
import './App.css'

function App() {
  return (
    <div
      className="App"
      style={{
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        height: '100vh',
      }}
    >
      POC Firebase Auth
    </div>
  )
}

export default App
